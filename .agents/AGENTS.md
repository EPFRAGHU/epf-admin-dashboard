# Git Auto-Sync Rule

- After completing any significant updates, features, or database synchronizations for this project, you must automatically run `git add .`, `git commit -m "[Description]"`, and `git push` without needing to be asked.
